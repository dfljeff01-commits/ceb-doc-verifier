name: ceb_verifier
description: 中欧班列单证核验 Android 现场版（拍照即传 → 一句话反馈 → 现场速查；完整处理在电脑端）
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  image_picker: ^1.1.0
  path_provider: ^2.1.6
  shared_preferences: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
