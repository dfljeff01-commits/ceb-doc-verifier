# -*- coding: utf-8 -*-
"""数据核对页（财务角色入口）——本轮仅占位，比对逻辑等待架构方单独下发任务书。

任务书明确（§7 范围外）：不实现"数据核对"模块的具体比对逻辑，不对账双方
数据来源与规则未明确前不做任何猜测性实现。本页只负责：
  - 入口与导航可见（财务/管理员角色）；
  - 说明建设状态与后续接入点。
"""

import streamlit as st


def render() -> None:
    st.header("📊 数据核对")
    st.caption("财务数据比对模块 · 建设中")

    st.info(
        "该功能正在建设中。\n\n"
        "数据核对模块将用于财务侧数据的比对核对，具体比对逻辑、对账双方的"
        "数据来源与核对规则，等待架构方明确后按任务书单独实施。",
        icon="🚧")

    st.markdown("#### 本轮已就绪的基础设施")
    st.markdown(
        "- 登录鉴权与角色体系（业务 / 财务 / 管理员）已上线，财务账号可正常登录；\n"
        "- 本模块的服务端与数据库基础（PostgreSQL + 操作留痕）已就绪；\n"
        "- 单据核对功能请由业务账号在「单据核对」入口使用。")

    st.divider()
    st.caption("架构方明确对账规则后，本页将替换为实际的数据核对功能。")
