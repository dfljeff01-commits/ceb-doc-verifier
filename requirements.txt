streamlit==1.64.0
pandas==3.0.5
reportlab==5.0.1
fastapi==0.141.1
uvicorn==0.53.0
# /ingest/image、/ingest/pdf 上传路由的必需依赖（缺失时 import api 直接失败，F06）
python-multipart==0.0.32
# serve_apk.py 二维码生成依赖（F06）
qrcode==8.2
pdfplumber==0.11.10
pymupdf==1.28.2
# 单据类型规则知识库 doc_rules.yaml 的解析依赖（任务书问题三；缺失时
# doc_rules 回退内置默认规则，引擎仍可运行）
pyyaml==6.0.3
pdf2image==1.17.0
pytesseract==0.3.13
pillow==12.3.0
# 数据核对模块v1：Excel 导入解析与模板生成（任务书 §五）
openpyxl==3.1.5
# ---- 架构升级新增：PostgreSQL + 登录鉴权 ----
# PostgreSQL 驱动（连接池见 db.py）
psycopg2-binary==2.9.10
# 密码哈希（bcrypt，绝不明文/可逆存储）
bcrypt==4.2.1
# 登录令牌（JWT HS256）
PyJWT==2.10.1
# 业务时区转换（容器内 zoneinfo 数据源）
tzdata==2025.2
# 网页端 → API 调用（此前经传递依赖可用，现固定版本）
requests==2.32.5
